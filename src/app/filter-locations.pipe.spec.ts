import { FilterLocationsPipe } from './filter-locations.pipe';

describe('FilterLocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
